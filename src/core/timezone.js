'use strict';

/**
 * core/timezone
 *
 * Timezone utilities for Millas.
 *
 * Usage:
 *   const { Time } = require('millas/core/timezone');
 *   const now = Time.now();
 */

const Time = require('../support/Time');

module.exports = { Time };
